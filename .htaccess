Options -MultiViews

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

# Alle Anfragen auf index.php umleiten, außer wenn Datei/Verzeichnis existiert
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

RewriteRule ^ index.php [QSA,L]
</IfModule>
